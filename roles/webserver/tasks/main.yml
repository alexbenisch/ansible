---
- name: Install nginx
  apt:
    name: nginx
    state: present

- name: Allow HTTP and HTTPS through UFW
  ufw:
    rule: allow
    port: "{{ item }}"
    proto: tcp
  loop:
    - "80"
    - "443"

- name: Start and enable nginx
  service:
    name: nginx
    state: started
    enabled: true
